## Running sum of Array

class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        
        for( int j=1;j<nums.size();j++)
        {
            nums[j]+=nums[j-1];
        }
        return nums;
    }
     
};
